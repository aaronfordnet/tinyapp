<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <% include ../views/partials/_header %>
  </head>
  <body>
    <ul>
        <% for (let url in urls) { %>
            <li><%= url %> - <%= urls[url] %></li>
        <% }; %>
    </ul>

    <a href="urls/new">Shorten a new URL</a>
  </body>
</html>
